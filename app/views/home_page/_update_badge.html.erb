<div>
  <a href="<%= link %>" class="inline-flex space-x-4">
    <span class="rounded bg-red-50 dark:bg-red-200 px-2.5 py-1 text-xs font-semibold text-red-500 tracking-wide uppercase">
      <%= title %>
    </span>
    <span class="inline-flex items-center text-sm font-medium text-red-500 dark:text-red-600 space-x-1">
      <span><%= body %></span>
      <!-- Heroicon name: solid/chevron-right -->
      <%= render 'icons/chevron-right', classes: 'h-5 w-5' %>
    </span>
  </a>
</div>
