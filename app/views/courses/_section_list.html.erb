<% unless section_list.empty? %>
  <%= react_component_transparent('SectionListCard',
                                  props: {
                                    isBetaTester: current_user&.beta_tester.presence || false,
                                    sectionsByTerm: section_list,
                                  }) %>
<% end %>
