<%= form_with(model: Relationship.new, remote: true) do |f| %>
  <div><%= hidden_field_tag :section_id, section.id %></div>
  <%= f.button("Follow", type: :submit, class: "button-primary") %>
<% end %>
